initial_velocity = float(input("initial_velocity: "))
final_velocity = float(input("final_velocity: "))
t = float(input("time span: "))

a = (final_velocity - initial_velocity) / t

print("a:", a)